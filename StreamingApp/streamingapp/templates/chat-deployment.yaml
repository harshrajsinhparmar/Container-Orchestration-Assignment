apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-chat
  labels:
    app: chat
spec:
  replicas: {{ .Values.services.chat.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: chat
  template:
    metadata:
      labels:
        app: chat
    spec:
      containers:
        - name: chat
          image: "{{ .Values.services.chat.image.repository }}:{{ .Values.services.chat.image.tag }}"
          imagePullPolicy: {{ .Values.services.chat.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.chat.port }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-config
            - secretRef:
                name: {{ .Release.Name }}-secret
          env:
            - name: PORT
              value: {{ .Values.services.chat.port | quote }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.services.chat.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: {{ .Values.services.chat.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: chat-svc
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.chat.port }}
      targetPort: {{ .Values.services.chat.port }}
  selector:
    app: chat
